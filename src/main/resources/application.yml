spring:
  flyway:
    validate-on-migrate: true
  datasource:
    url: jdbc:mysql://localhost:3306/storcars?useTimezone=true&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
